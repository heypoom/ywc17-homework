doctype html
html(lang="th-TH")
  head
    link(rel="stylesheet", href="../styles/index.sass")
  body
    header.navbar
    section.hero
      .cover.left
      .cover.center
        .hero-logo
      .cover.right
    main
      section.primary
        .since ตั้งแต่วันที่
        .duration.fade.in
        a.register-box(href="https://regist.ชิมช้อปใช้.com/Register", target="_blank")
          p ลงทะเบียน เฟส 2
          p ตั้งแต่วันที่ 24 ต.ค. 62 วันละ 2 รอบ เวลา 6.00 น. และ 18.00 น.
          p (จำกัดจำนวนผู้ลงทะเบียนรอบละ 5 แสนคน รวม 1 ล้านคนต่อวัน)
        hr

      section.details
        h1
          p มาตรการส่งเสริมการบริโภค
          p ในประเทศ “ชิมช้อปใช้”
        p.detail

        h3 เงื่อนไขการเข้าร่วมมาตรการ
        p.condition

      section.hotlines
        - const numbers = ['021111144', '022706400', '1672']
          for hotline, idx in ['KTB', 'CGD', 'TAT']
            a(href="tel:" + numbers[idx])
              img.banner(src="../images/hotlines/" + hotline + ".png")

    script(src="../scripts/index.ts")
